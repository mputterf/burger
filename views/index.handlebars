<h1>Burgers</h1>
<ul>
    {{#each burgers}}
    {{#unless devoured}}

    {{burger_name}} <button class="eatBurger" id="{{this.id}}" data-devoured="{{this.devoured}}">Eat It!</button><br>

    {{/unless}}
    {{/each}}
</ul>

<h1>Add a burger!</h1>
<form class="addBurgerForm">
    <input type="text" id="burgerAddText" name="burgerName"><br>
    <button type="submit" name="burgerAddButton">Add It!</button>
</form>

<h1>Devoured Burgers</h1>
<ul>
    {{#each burgers}}
    {{#if devoured}}
    {{burger_name}}
    {{/if}}
    {{/each}}
</ul>